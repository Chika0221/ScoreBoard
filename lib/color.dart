// Flutter imports:
import 'package:flutter/material.dart';

class ScoreBoardColors {
  final colorScheme = ColorScheme(
    brightness: Brightness.light,
    primary: Colors.black,
    onPrimary: Colors.white,
    secondary: Colors.blueGrey,
    onSecondary: Colors.white,
    error: Colors.red,
    onError: Colors.white,
    surface: Colors.white,
    onSurface: Colors.black,
  );

  final customColor1 = Colors.red.withAlpha(100);
  final customColor2 = Colors.blue.withAlpha(100);
  final customColor3 = Colors.green.withAlpha(100);
  final customColor4 = Colors.orange.withAlpha(100);
  final customColor5 = Colors.purple.withAlpha(100);
}
